# -*- sh -*-

# Useful functions

mcd () { mkdir -p "$1"; cd "$1"; }

odx () { od -t x4 -A x "$@" ; }

odx1 () { od -t x1 -A x "$@" ; }

fch () { find . -type f -name "*.[ch]" | xargs grep "$@" }

fchs () { find . -type f -name "*.[chsS]" | xargs grep "$@" }

# Git related handy-handy stuff

make-project () { mcd "$1"; git init; cp /usr/share/common-licenses/$2 COPYING; git add COPYING; git commit -m "This project is under the $2 licence at this time"; }

# Haskell related handy stuff

cabal-dev-here () { CABAL_SANDBOX="$(pwd)/cabal-dev"; export CABAL_SANDBOX; }

# We prevented ^D == EOF earlier (30-settings) so now, let's have a clever
# ^D handler which does normal ^D behaviour, and also turns exit into popd
# if the stack isn't empty...
clever_logout_widget () {
    if test "x$BUFFER" != "x"; then
	# Buffer is non-empty, need to do usual ^D behaviour
	# CURSOR is the offer into BUFFER, we need to remove the character
	# at CURSOR in BUFFER
	local BUFLEFT BUFRIGHT
	BUFLEFT="$BUFFER[0,$CURSOR]"
	BUFRIGHT="$BUFFER[$((CURSOR + 2)),-1]"
	BUFFER="${BUFLEFT}${BUFRIGHT}"
	zle -R
    elif test "x$dirstack" != "x"; then
	# Need to popd
	popd 2>/dev/null >/dev/null
	zle -I
	zle -R
    else
	# Need to exit
	exit
    fi
}

zle -N clever_logout_widget

bindkey '^D' clever_logout_widget

# Alias Functions

function fr { grep -rl "$1" "$3" | xargs sed -i "s/$1/$2/g" }

# ---------------------------------------------------------------------------
# Alias cheatsheets — mirrors pwsh's aliases / gitaliases / allaliases
# ---------------------------------------------------------------------------

# Renders a list of "alias\x1fcommand\x1fdescription" entries (or
# "SECTION\x1fname" section-header entries) as an aligned, coloured table.
_print_alias_cheatsheet() {
    local title="$1"; shift
    local -a entries=("$@")
    local esc=$'\033' reset bold yellow cyan gray
    reset="${esc}[0m"; bold="${esc}[1m"; yellow="${esc}[33m"; cyan="${esc}[36m"; gray="${esc}[90m"

    local entry a rest c d
    local alias_width=0 cmd_width=0
    for entry in "${entries[@]}"; do
        [[ "$entry" == SECTION$'\x1f'* ]] && continue
        a="${entry%%$'\x1f'*}"
        rest="${entry#*$'\x1f'}"
        c="${rest%%$'\x1f'*}"
        (( ${#a} > alias_width )) && alias_width=${#a}
        (( ${#c} > cmd_width ))   && cmd_width=${#c}
    done
    (( alias_width += 2 ))
    (( cmd_width += 2 ))

    echo ""
    echo "  ${bold}${cyan}${title}${reset}"
    echo "  ${gray}${(r:$((alias_width + cmd_width + 20))::─:)}${reset}"

    for entry in "${entries[@]}"; do
        if [[ "$entry" == SECTION$'\x1f'* ]]; then
            echo ""
            echo "  ${yellow}${entry#SECTION$'\x1f'}${reset}"
            continue
        fi
        a="${entry%%$'\x1f'*}"
        rest="${entry#*$'\x1f'}"
        c="${rest%%$'\x1f'*}"
        d="${rest#*$'\x1f'}"
        printf "    %s${gray}%s${reset}%s\n" "${(r:$alias_width:)a}" "${(r:$cmd_width:)c}" "$d"
    done
    echo ""
}

aliases() {
    local -a e=(
        "SECTION"$'\x1f'"File system"
        "l"$'\x1f'"ls -CF"$'\x1f'"quick columnar listing with type indicators"
        "ll"$'\x1f'"ls -l"$'\x1f'"long listing"
        "la"$'\x1f'"ls -a"$'\x1f'"include dotfiles"
        "lla"$'\x1f'"ls -al"$'\x1f'"long listing including dotfiles"
        "ls"$'\x1f'"ls --color=auto"$'\x1f'"colorized (plain ls if dircolors isn't installed)"
        "md"$'\x1f'"mkdir"$'\x1f'"make a directory"
        "mcd <dir>"$'\x1f'"mkdir -p && cd"$'\x1f'"create a directory and cd into it in one step"
        "tre"$'\x1f'"tree -I ..."$'\x1f'"directory tree, excluding build/vcs noise dirs"
        "v <file>"$'\x1f'"vim"$'\x1f'"open in vim"
        "vb"$'\x1f'"vim rc.d/"$'\x1f'"edit this zsh config"
        "odx <file>"$'\x1f'"od -t x4 -A x"$'\x1f'"4-byte hex dump"
        "odx1 <file>"$'\x1f'"od -t x1 -A x"$'\x1f'"1-byte hex dump"
        "remspace"$'\x1f'"sed -i ..."$'\x1f'"strip trailing whitespace from the current dir's files"
        "remspace_dir"$'\x1f'"(custom)"$'\x1f'"strip trailing whitespace, dir-wide"
        "rm  rmr"$'\x1f'"mv to ~/.trash"$'\x1f'"delete a file/dir (recoverable — see trash-restore)"
        "trash-restore <name>"$'\x1f'"(custom)"$'\x1f'"bring something back out of ~/.trash"
        "trash-empty"$'\x1f'"(custom)"$'\x1f'"permanently clear ~/.trash (confirms first)"
        "cp  mv"$'\x1f'"cp -i / mv -i"$'\x1f'"confirm before overwriting a file"

        "SECTION"$'\x1f'"Navigation"
        "cdh"$'\x1f'"cd ~"$'\x1f'"home"
        ".. / ... / ...."$'\x1f'"cd .. / ../.. / ../../.."$'\x1f'"step up one/two/three directories"
        "rp"$'\x1f'"cd ~/repos"$'\x1f'"repos root"
        "rpz  rpp"$'\x1f'"cd ~/repos/{zsh,pwsh}"$'\x1f'"this project / the pwsh sibling project"
        "rpsource"$'\x1f'"cd ~/.resources/zsh"$'\x1f'"the deployed config (symlinked to rpz)"
        "rps  rpa  rpam"$'\x1f'"cd ~/repos/..."$'\x1f'"project shortcuts (ShatteredRealms, DriveFurtherAPI)"
        "rpn  rpnm  rpc"$'\x1f'"cd ~/repos/..."$'\x1f'"more project shortcuts"
        "rpm  rpnc"$'\x1f'"cd ~/repos/..."$'\x1f'"more project shortcuts"
        "cd -N  cd -<Tab>"$'\x1f'"pushd/popd (automatic)"$'\x1f'"hop back through recently-visited directories"
        "z <partial-name>"$'\x1f'"zoxide (if installed)"$'\x1f'"jump to a frecently-visited directory"

        "SECTION"$'\x1f'"Search"
        "g <pattern>"$'\x1f'"grep -rniI ."$'\x1f'"recursive search, common noise dirs excluded"
        "g1 <pattern>"$'\x1f'"grep (full excludes)"$'\x1f'"recursive search, fuller exclusion list"
        "g2 <pattern>"$'\x1f'"grep (excl. + bin)"$'\x1f'"recursive search, excludes + bin/"
        "rgs <pattern>"$'\x1f'"rg -n"$'\x1f'"ripgrep search, common noise dirs excluded"
        "rgs1 <pattern>"$'\x1f'"rg -n (minimal)"$'\x1f'"ripgrep, minimal exclusions"
        "rgs2 <pattern>"$'\x1f'"rg -n (broader)"$'\x1f'"ripgrep, excludes node_modules too"
        "fch <pattern>"$'\x1f'"find *.c/*.h | grep"$'\x1f'"search C/H source files"
        "fchs <pattern>"$'\x1f'"find *.c/*.h/*.s/*.S"$'\x1f'"search C/H/asm source files"

        "SECTION"$'\x1f'"Shell utilities"
        "bye"$'\x1f'"exit"$'\x1f'"leave the shell"
        "r"$'\x1f'"reset"$'\x1f'"clear/reset the terminal"
        "ng"$'\x1f'"grep -v"$'\x1f'"invert-match grep"
        "sb"$'\x1f'"source rc"$'\x1f'"reload this zsh config"
        "fr <find> <repl> [path]"$'\x1f'"(custom)"$'\x1f'"find & replace across files"
        "path"$'\x1f'"(custom)"$'\x1f'"print \$PATH one entry per line"
        "psgrep <name>"$'\x1f'"(custom)"$'\x1f'"find a running process by partial name"
        "brk"$'\x1f'"(custom)"$'\x1f'"horizontal rule across the terminal"
        "c"$'\x1f'"claude"$'\x1f'"launch Claude Code"
        "open <path>"$'\x1f'"xdg-open"$'\x1f'"launch a file/URL with the default app"
        "copy"$'\x1f'"wl-copy/xclip/xsel"$'\x1f'"pipe stdin to the system clipboard (if installed)"
        "cheat  allaliases"$'\x1f'"(custom)"$'\x1f'"show this cheatsheet (aliases + gitaliases)"
        "zsh-doctor"$'\x1f'"(custom)"$'\x1f'"show which optional integrations (fzf, bat, ...) are active"
        "view <file>"$'\x1f'"bat (if installed)"$'\x1f'"syntax-highlighted, paged file viewer"

        "SECTION"$'\x1f'"Project / workspace"
        "make-project <name> <licence>"$'\x1f'"(custom)"$'\x1f'"git init + add a licence file"
        "dbuild"$'\x1f'"docker run ..."$'\x1f'"buildstream dev container"
        "repos-status [root]"$'\x1f'"(custom)"$'\x1f'"scan ~/repos for uncommitted/unpushed work"
    )
    _print_alias_cheatsheet "General aliases" "${e[@]}"
}

gitaliases() {
    local -a e=(
        "SECTION"$'\x1f'"Status & info"
        "gstat  g!  git!"$'\x1f'"git status (custom)"$'\x1f'"colour-coded status with branch and ahead/behind"
        "gbl"$'\x1f'"git blame"$'\x1f'"annotate each line with last-changing commit"
        "gs  gits"$'\x1f'"git show"$'\x1f'"inspect a commit, tag, or blob"
        "gitsn"$'\x1f'"git show --name-only"$'\x1f'"list files changed in a commit"
        "gsl"$'\x1f'"git show | less"$'\x1f'"inspect a commit, paged"
        "rs"$'\x1f'"clear; git status"$'\x1f'"clear screen then show status"

        "SECTION"$'\x1f'"Log"
        "gl  gitl"$'\x1f'"git log"$'\x1f'"pretty full log, scrollable via less"
        "gl2  gitl2"$'\x1f'"git log --shortstat"$'\x1f'"full log with per-commit file-change counts"
        "gitlg"$'\x1f'"git log --graph"$'\x1f'"branching graph log"
        "gitlg2"$'\x1f'"git log --oneline --graph"$'\x1f'"compact one-line graph log"

        "SECTION"$'\x1f'"Staging"
        "ga  gita"$'\x1f'"git add"$'\x1f'"stage files"
        "gau  gitau"$'\x1f'"git add -u"$'\x1f'"stage all changes to tracked files"
        "gaw"$'\x1f'"diff -w | apply --cached"$'\x1f'"stage all changes, ignoring whitespace"
        "gaw1 <pattern>"$'\x1f'"apply --cached --include"$'\x1f'"stage matching files, ignoring whitespace"

        "SECTION"$'\x1f'"Diff"
        "gdf"$'\x1f'"git diff"$'\x1f'"show unstaged changes"
        "gdfs"$'\x1f'"git diff --staged"$'\x1f'"show staged changes"
        "gdf1"$'\x1f'"git diff HEAD~1"$'\x1f'"show changes introduced by the last commit"

        "SECTION"$'\x1f'"Commit"
        "gc"$'\x1f'"git commit"$'\x1f'"open editor and commit"
        "gitcm <msg>"$'\x1f'"git commit -m"$'\x1f'"commit with an inline message"
        "gitca  gca"$'\x1f'"git commit --amend"$'\x1f'"rewrite the last commit"
        "uncommit"$'\x1f'"git reset --soft HEAD^"$'\x1f'"undo last commit, keep changes staged"

        "SECTION"$'\x1f'"Restore & reset"
        "gres  gitres"$'\x1f'"git checkout --"$'\x1f'"discard working tree changes to a file"
        "gress"$'\x1f'"git restore --staged"$'\x1f'"move staged changes back to working tree"
        "grh [commit]"$'\x1f'"git reset --hard"$'\x1f'"discard all changes, optionally to a commit (confirms first)"
        "gup"$'\x1f'"git reset HEAD~1"$'\x1f'"undo last commit, leave changes unstaged"

        "SECTION"$'\x1f'"Archive & ignore"
        "gar [name]"$'\x1f'"git archive HEAD"$'\x1f'"zip HEAD into <name>-<hash>.zip"
        "localignore <pattern>"$'\x1f'".git/info/exclude"$'\x1f'"ignore files locally without touching .gitignore"
        "ignoreme"$'\x1f'"update-index --assume-unchanged"$'\x1f'"stop tracking local changes to a file"
        "dontignoreme"$'\x1f'"update-index --no-assume-unchanged"$'\x1f'"resume tracking local changes to a file"
        "gitrmc"$'\x1f'"git rm --cached"$'\x1f'"remove a file from the index only"

        "SECTION"$'\x1f'"Branch & checkout"
        "gitb"$'\x1f'"git branch"$'\x1f'"list or manage branches"
        "gitc <branch>"$'\x1f'"git checkout"$'\x1f'"switch branch or restore files"
        "gitcb <branch>"$'\x1f'"git checkout -b"$'\x1f'"create and switch to a new branch"
        "gitcd"$'\x1f'"git checkout develop"$'\x1f'"switch to develop"
        "gitcf"$'\x1f'"fzf (if installed)"$'\x1f'"fuzzy-pick a local/remote branch and check it out"

        "SECTION"$'\x1f'"Stash"
        "gst  stash"$'\x1f'"git stash push"$'\x1f'"save dirty state onto the stash"
        "gp  stashp"$'\x1f'"git stash pop"$'\x1f'"restore the most recent stash entry"
        "gstl"$'\x1f'"git stash list"$'\x1f'"list stash entries"

        "SECTION"$'\x1f'"Cherry-pick & clean"
        "gitcp <commit>"$'\x1f'"git cherry-pick"$'\x1f'"apply a commit onto the current branch"
        "gclean"$'\x1f'"git clean -fd"$'\x1f'"delete untracked files and directories (confirms first)"

        "SECTION"$'\x1f'"Fetch & pull"
        "gitf"$'\x1f'"git fetch"$'\x1f'"download changes from remote"
        "gitpl"$'\x1f'"git pull --rebase"$'\x1f'"fetch and rebase onto upstream"
        "update"$'\x1f'"git fetch + rebase"$'\x1f'"sync current branch with origin"
        "updated"$'\x1f'"git fetch + rebase"$'\x1f'"sync with origin/develop"

        "SECTION"$'\x1f'"Push"
        "gpf  gitfp  gitpf"$'\x1f'"git push --force"$'\x1f'"force-push (confirms first)"
        "gp1"$'\x1f'"git push --set-upstream"$'\x1f'"push and set upstream tracking"
        "pushdr <branch>"$'\x1f'"push -u origin local:remote"$'\x1f'"push to a differently-named remote branch"
        "pushdr-f <branch>"$'\x1f'"push -uf origin local:remote"$'\x1f'"force-push to a differently-named remote branch"

        "SECTION"$'\x1f'"Remote"
        "gru"$'\x1f'"git remote update"$'\x1f'"fetch all remotes"
        "grpo"$'\x1f'"git remote prune origin"$'\x1f'"remove stale remote-tracking refs"
        "giturl"$'\x1f'"config --get remote.origin.url"$'\x1f'"print the remote URL"
        "gitremote-reset  remote!"$'\x1f'"git fetch + reset --hard"$'\x1f'"hard-reset current branch to remote HEAD"

        "SECTION"$'\x1f'"Rebase"
        "gri [n|commit]"$'\x1f'"git rebase -i"$'\x1f'"interactively rewrite history"
        "grim [n|commit]"$'\x1f'"rebase -i --rebase-merges"$'\x1f'"interactive rebase, preserving merge commits"
        "grd"$'\x1f'"git rebase develop"$'\x1f'"rebase current branch onto develop"
        "grm"$'\x1f'"git rebase master"$'\x1f'"rebase current branch onto master"
        "gro"$'\x1f'"git fetch + rebase FETCH_HEAD"$'\x1f'"sync and rebase onto origin/<current>"
        "groi"$'\x1f'"git fetch + rebase -i FETCH_HEAD"$'\x1f'"sync and interactive rebase onto origin/<current>"
        "grod"$'\x1f'"git fetch + rebase FETCH_HEAD"$'\x1f'"sync and rebase onto origin/develop"
        "grof <branch>"$'\x1f'"git fetch + rebase FETCH_HEAD"$'\x1f'"sync and rebase onto named remote branch"
        "rebaseon  rebase <fragment>"$'\x1f'"git rebase"$'\x1f'"rebase onto branch matching partial name"
        "gitra"$'\x1f'"git rebase --abort"$'\x1f'"abort an in-progress rebase"
        "gitrc"$'\x1f'"git rebase --continue"$'\x1f'"continue after resolving conflicts"
        "groot"$'\x1f'"rebase -i --root master"$'\x1f'"rebase all commits from the very first"

        "SECTION"$'\x1f'"Reverted changes"
        "grev <A> <B>"$'\x1f'"(custom)"$'\x1f'"show changes A made that B later undid"
        "grev-clean <A> <B>"$'\x1f'"(custom)"$'\x1f'"rewrite A to remove those redundant changes"
        "grev-scan [range]"$'\x1f'"(custom)"$'\x1f'"scan a commit range for changes later reverted"

        "SECTION"$'\x1f'"Worktree"
        "wt-list"$'\x1f'"git worktree list"$'\x1f'"list all worktrees"
        "wt-go <name>"$'\x1f'"cd"$'\x1f'"cd to a worktree by name fragment"
        "wt-gof"$'\x1f'"fzf (if installed)"$'\x1f'"fuzzy-pick a worktree and cd to it"
        "wt-feature <id> <desc>"$'\x1f'"worktree add -b feature/..."$'\x1f'"create a feature branch worktree"
        "wt-fix <id> <desc>"$'\x1f'"worktree add -b fix/..."$'\x1f'"create a fix branch worktree"
        "wt-c <branch>"$'\x1f'"git worktree add"$'\x1f'"checkout an existing branch as a worktree"
        "wt-done <name>"$'\x1f'"worktree remove + branch -d"$'\x1f'"remove a worktree and delete its local branch"
        "wt-done-f <name>"$'\x1f'"worktree remove -f + branch -D"$'\x1f'"force-remove a worktree and delete its branch"
        "wt!"$'\x1f'"git status (all worktrees)"$'\x1f'"show status across every worktree"
        "wt-prune"$'\x1f'"git worktree prune"$'\x1f'"clean up stale worktree references"
        "wt-setup <branches>"$'\x1f'"(custom)"$'\x1f'"single-repo bare clone with branch worktrees"
        "wt-workspace <path> <repos> <branches>"$'\x1f'"(custom script)"$'\x1f'"multi-repo workspace with per-repo worktrees"
        "fix-fetch-refspecs [root]"$'\x1f'"config + remote update"$'\x1f'"repair missing fetch refspecs across repos"
    )
    _print_alias_cheatsheet "Git aliases" "${e[@]}"
}

allaliases() {
    aliases
    gitaliases
}

# ---------------------------------------------------------------------------
# zsh-doctor — report which optional, tool-gated integrations are active.
# Several features in this config (colorized man pages, fzf pickers, zoxide,
# direnv, desktop notifications, clipboard) are inert until their underlying
# tool is installed — this is the one place to see, at a glance, which of
# those are actually live right now.
# ---------------------------------------------------------------------------
zsh-doctor() {
    local esc=$'\033' reset green red gray
    reset="${esc}[0m"; green="${esc}[32m"; red="${esc}[31m"; gray="${esc}[90m"

    echo ""
    echo "  Optional integrations"
    echo "  ----------------------"

    local -a checks=(
        "dircolors:colorized ls / tab-completion menu"
        "bat:colorized man pages, view"
        "fzf:Ctrl+R / Ctrl+T, wt-gof, gitcf"
        "zoxide:z <partial-name>"
        "direnv:per-project .envrc auto-loading"
        "notify-send:desktop notification for long commands"
        "bzr:bzr repo detection in the prompt"
        "svn:svn repo detection in the prompt"
    )
    local check tool label
    for check in "${checks[@]}"; do
        tool="${check%%:*}"
        label="${check#*:}"
        if (( ${+commands[$tool]} )); then
            echo "  ${green}✓${reset} ${tool}  ${gray}${label}${reset}"
        else
            echo "  ${red}✗${reset} ${tool}  ${gray}${label} — not installed${reset}"
        fi
    done

    echo ""
    local clip="none found"
    (( ${+commands[wl-copy]} )) && clip="wl-copy"
    [[ "$clip" == "none found" ]] && (( ${+commands[xclip]} )) && clip="xclip"
    [[ "$clip" == "none found" ]] && (( ${+commands[xsel]} )) && clip="xsel"
    if [[ "$clip" == "none found" ]]; then
        echo "  ${red}✗${reset} clipboard (copy)  ${gray}none of wl-copy/xclip/xsel found${reset}"
    else
        echo "  ${green}✓${reset} clipboard (copy)  ${gray}using ${clip}${reset}"
    fi
    echo ""
}

# view — read-only, syntax-highlighted file viewer via bat (falls back to
# less if bat isn't installed). Opt-in name so plain `cat` behaviour and
# any scripts relying on it are untouched.
if (( ${+commands[bat]} )); then
    view() { bat --paging=always "$@"; }
else
    view() { less "$@"; }
fi

